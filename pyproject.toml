[project]
name = "saludo-equipo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "cmilogv@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
]

[project.scripts]
saludo-equipo = "saludo_equipo:main"
fecha = "saludo_equipo.fecha:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
